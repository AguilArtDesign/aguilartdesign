<footer class="footer">

    <div class="container footer-main">

        <div class="footer-brand">

            <a
                href="#inicio"
                class="brand"
                aria-label="Volver al inicio"
            >
                <span class="brand-logo" aria-hidden="true"></span>
            </a>


            <p>
                Soluciones digitales sobre WordPress para empresas que necesitan
                algo más que una página web.
            </p>

        </div>


        <nav aria-label="Navegación del pie de página">

            <a href="#soluciones">
                Soluciones
            </a>

            <a href="#capacidades">
                Capacidades
            </a>

            <a href="#proceso">
                Proceso
            </a>

            <a href="#contacto">
                Contacto
            </a>

        </nav>

    </div>


    <div class="container footer-bottom">

        <span>
            © 2026 AguilArt Design
        </span>

        <span>
            Transformando ideas en experiencias digitales.
        </span>

    </div>

</footer>


<style>

    .footer {
        padding:
            58px 0
            24px;

        background:
            var(--footer-bg);

        border-top:
            1px solid
            var(--border);

        transition:
            background-color var(--transition-theme),
            border-color var(--transition-theme);
    }


    /* =====================================================
       MAIN
       ===================================================== */

    .footer-main {
        display: grid;

        grid-template-columns:
            1fr auto;

        gap: 50px;

        align-items: start;
    }


    /* =====================================================
       BRAND
       ===================================================== */

    .brand {
        display: inline-flex;

        align-items: center;

        color: var(--text);
    }


    .brand-logo {
        display: block;
        width: 150px;
        aspect-ratio: 509.43 / 145.18;

        background-color: var(--text);

        -webkit-mask: url("/media/Logo.svg") center / contain no-repeat;
        mask: url("/media/Logo.svg") center / contain no-repeat;

        transition: background-color var(--transition-theme);
    }


    .footer-main p {
        max-width: 460px;

        margin:
            18px 0
            0;

        color:
            var(--footer-description);

        font-size: 12px;

        line-height: 1.75;

        transition:
            color var(--transition-theme);
    }


    /* =====================================================
       NAVIGATION
       ===================================================== */

    .footer-main nav {
        display: flex;

        gap: 25px;

        color:
            var(--footer-nav);

        font-size: 11px;
    }


    .footer-main nav a {
        transition:
            color 0.2s ease;
    }


    .footer-main nav a:hover {
        color:
            var(--footer-nav-hover);
    }


    /* =====================================================
       BOTTOM
       ===================================================== */

    .footer-bottom {
        display: flex;

        align-items: center;
        justify-content: space-between;

        gap: 30px;

        margin-top: 42px;

        padding-top: 24px;

        color:
            var(--footer-bottom);

        border-top:
            1px solid
            var(--footer-bottom-border);

        font-size: 10px;

        transition:
            color var(--transition-theme),
            border-color var(--transition-theme);
    }


    /* =====================================================
       MOBILE
       ===================================================== */

    @media (max-width: 767px) {

        .footer {
            padding-top: 48px;
        }


        .footer-main {
            grid-template-columns: 1fr;

            gap: 36px;
        }

        .brand-logo {
            width: 136px;
        }


        .footer-main nav {
            flex-wrap: wrap;

            gap:
                16px 24px;
        }


        .footer-bottom {
            align-items: flex-start;
            flex-direction: column;

            gap: 8px;

            margin-top: 36px;
        }

    }

</style>
